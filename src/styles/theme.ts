import { caveat, poppins, raleway } from "@/pages/_app";
import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    typography: {
      fontFamily: poppins.style.fontFamily,
    },
  })